{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run Game",
            "type": "debugpy",
            "request": "launch",
            "program": "${workspaceFolder}/src/game.py",
            "cwd": "${workspaceFolder}/src", // workspace root
            "console": "integratedTerminal",
            "justMyCode": true
        }
    ]
}